\subsubsection{Cấu trúc gói tin JSON truyền MQTT}
Gói tin dữ liệu được vi điều khiển ESP32 đóng gói và truyền tải qua giao thức MQTT sử dụng định dạng JSON chuẩn. Dưới đây là đặc tả các trường thông tin trong cấu trúc gói tin gửi lên topic \texttt{aqistation/data}:

\begin{table}[H]
    \centering
    \caption{Cấu trúc dữ liệu gói tin JSON truyền MQTT}
    \label{tab:json_structure}
    
\end{table}

